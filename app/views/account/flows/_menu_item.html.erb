<% if can? :read, Flow.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :flows]),
    label: t('flows.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('flows.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
