<% if can? :read, Verb.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :verbs]),
    label: t('verbs.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('verbs.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
