en:
  operands: &operands
    label: &label Operands
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-puzzle-piece
    buttons: &buttons
      new: Add New Operand
      create: Create Operand
      edit: Edit Operand
      update: Update Operand
      destroy: Remove Operand
      shorthand:
        edit: Edit
        destroy: Delete
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to remove %{operand_name}? This will also remove any child resources and can't be undone.
    operand:
      buttons: *buttons
    fields: &fields
      id:
        heading: Operand ID
      act_id:
        heading: Act ID
      name:
        _: &name Name
        label: *name
        heading: *name
        api_title: *name
        api_description: *name
      description:
        _: &description Description
        label: *description
        heading: *description
        api_title: *description
        api_description: *description
      noun_id: &noun
        _: &noun_id Noun
        label: *noun_id
        heading: *noun_id
        api_title: *noun_id
        api_description: *noun_id
        placeholder: None
      noun: *noun
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        _: &created_at Added
        label: *created_at
        heading: *created_at
      updated_at:
        _: &updated_at Updated
        label: *updated_at
        heading: *updated_at
    api:
      collection_actions: "Collection Actions for Operands"
      index: "List Operands"
      create: "Add a New Operand"
      member_actions: "Actions for an Individual Operand"
      show: "Retrieve a Operand"
      update: "Update a Operand"
      destroy: "Delete a Operand"
      fields: *fields
    index:
      section: "%{acts_possessive} Operands"
      contexts:
        act:
          header: Operands
          description: Below is a list of Operands that have been added for %{act_name}.
          description_empty: No Operands have been added for %{act_name}.
      fields: *fields
      buttons: *buttons
    show:
      section: "%{operand_name}"
      header: Operand Details
      description: Below are the details we have for %{operand_name}.
      manage_description: You'll also find options for updating these details or removing %{operand_name} from %{act_name} entirely.
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    new:
      section: "New Operand for %{act_name}"
      header: New Operand Details
      description: Please provide the details of the new Operand you'd like to add to %{act_name}.
      form: *form
    edit:
      section: "%{operand_name}"
      header: Edit Operand Details
      description: You can update the details or settings for %{operand_name} below.
      form: *form
    notifications:
      created: Operand was successfully created.
      updated: Operand was successfully updated.
      destroyed: Operand was successfully destroyed.
  account:
    operands: *operands
  activerecord:
    attributes:
      operand:
        name: *name
        noun_id: *noun_id
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
        updated_at: *updated_at
